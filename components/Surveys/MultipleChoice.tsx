import { survey } from "@/lib/types"


const MultipleChoice = ({ survey } : { survey: survey }) => {

    return (
        <div className="custom-border container-paddings bg-white max-w-2xl w-full max-h-3xl h-[50vh] shadow-md">
            
        </div>
    )

}

export default MultipleChoice
